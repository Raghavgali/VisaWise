# FINAL held-out test: guardrail prompt v4 on Gemini 3.1 Flash-Lite, curated_v1.
# v4 was tuned only on dev_v1 (never on curated_v1), so these curated numbers are
# an honest measurement, not a fit target. Serving retrieval config, gpt-4o-mini
# judge (independent of the Gemini generator).
name: curated_v4_gemini
dataset: curated_v1
metrics: all
engine:
  retriever: hybrid
  top_k: 8
  vector_weight: 0.6
  reranker: local
  rerank_top_n: 4
  prompt_version: v4
  llm: gemini:gemini-3.1-flash-lite
  thinking_budget: 0
