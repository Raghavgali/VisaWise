# The production configuration [validated 2024: hybrid 0.6/0.4 + rerank].
name: hybrid_default
dataset: synthetic_v1
metrics: all
engine:
  retriever: hybrid
  top_k: 8
  vector_weight: 0.6
  reranker: local
  rerank_top_n: 4
